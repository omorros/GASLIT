# GASLIT — Python dependencies
# Single shared file. Teammates: add lines, do not delete.

# ─── MongoDB ───────────────────────────────────────────────────────────
pymongo>=4.10.0
motor>=3.6.0

# ─── AI providers ──────────────────────────────────────────────────────
anthropic>=0.40.0
voyageai>=0.3.0
openai>=1.50.0   # OpenAI-compatible client for Fireworks AI + Nemotron

# ─── LangChain / LangGraph ─────────────────────────────────────────────
langgraph>=0.6.0
langgraph-checkpoint-mongodb==0.3.1
langgraph-store-mongodb>=0.1.0
langsmith>=0.1.140
langchain-core>=0.3.0
langchain-anthropic>=0.3.0

# ─── Web framework ─────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=14.0
httpx>=0.27.0
python-multipart>=0.0.12

# ─── Utilities ─────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.9.0
numpy>=2.0.0

# ─── Teammate 1 (NVIDIA Sentinel) — install on their machine, not Oriol's ──
# nemoguardrails>=0.11.0   # needs C++ build tools on Windows; Teammate 1 manages

# ─── Teammate 2 (Voice) — LiveKit + ElevenLabs ─────────────────────────────
livekit-api>=0.8.0
elevenlabs>=1.50.0
